# Trade-show offline snapshot (wallpaper)

Curated **local-only** data for `demo/trade-show-offline`. Paths are `./data/...` relative to this folder so `COLORFLEX_DATA_BASE_URL = /demo-snapshot` resolves correctly.

Regenerate rasters + JSON (after editing `scripts/build-trade-show-snapshot.js`):

```bash
npm run build:trade-show-snapshot
```
