# Dependencies
node_modules/

# Python venv (Bassett PSD pipeline)
.venv/
.venv-b2/
# Bassett PSD pipeline output (scripts/bassett_psd_export.py)
bassett_psd_output/
# Bassett Photoshop JSX bridge (scripts/run_bassett_photoshop.sh)
bassett_psd/bassett_psd_bridge.txt
bassett_psd/bassett_psd_done.txt

# Local backups (separate from Git history)
backups/

# Theme pulls from Shopify (temporary; re-pull anytime)
theme-pull/
theme-pull-*/

# Nested clone / separate app (has its own .git — do not add as subfolder)
colorflex-previews/

# Secrets and local config
.env
*.env
!*.env.example
# Script backups (often duplicate secrets; do not commit)
src/scripts/cf-dl.js.backup*
config/shopify.json
config/local.env

# Local IDE / agent (Continue, etc.)
.continue/

# OS and editors
.DS_Store
*.swp
*.swo
*~

# Logs and temp
*.log
# Blender can produce huge text logs; GitHub limit is 100MB per file
src/blender/render-log.txt
tmp/

# Optional: uncomment if you don't want to track built/minified assets
# src/assets/*.min.js
# src/assets/*.min.css

# Assembled Windows golden-master handoff (copy to PC; npm install there)
ColorFlexTradeShow/

# Local homepage / marketing video assets (not in theme repo)
homePageVideos/
